# Database Configuration
POSTGRES_USER=limogiaicryptox
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
POSTGRES_DB=limogiaicryptox

# Kraken API (for live trading)
# Get from: https://www.kraken.com/u/security/api
KRAKEN_API_KEY=
KRAKEN_API_SECRET=

# Application Settings
LOG_LEVEL=INFO

# ===========================================
# API Authentication (REQUIRED for security)
# ===========================================
# Generate a key with: python -c "import secrets; print(secrets.token_hex(16))"
# This protects trading endpoints from unauthorized access
# API key for frontend to access protected endpoints
# Must match LIMOGI_API_KEY above
# Backend and frontend must use the same key
REACT_APP_API_KEY=
LIMOGI_API_KEY=

# Set to false only for local development without network exposure
AUTH_ENABLED=true

# Frontend
REACT_APP_API_URL=http://localhost:8000
REACT_APP_WS_URL=ws://localhost:8000


